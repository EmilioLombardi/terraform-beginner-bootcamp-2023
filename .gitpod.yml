tasks:
  - name: terraform
    before: |
      source ./bin/install_aws_cli
  - name: aws-cli
    env:
      AWS_CLI_AUTO_PROMPT: on-partial
    before: |
   
  extensions:
    - amazonwebservices.aws-toolkit-vscode
    - hashicorp.terraform